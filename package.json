{
  "name": "monorepo-template",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:marketing": "npm run dev -w packages/marketing",
    "lint": "npm run lint -w packages/marketing",
    "lint:marketing": "npm run lint -w packages/marketing",
    "typecheck": "npm run typecheck -w packages/marketing",
    "set-gitlab-vars": "tsx scripts/set-gitlab-vars.ts"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "typescript": "^5"
  },
  "dependencies": {
    "sst": "^3"
  }
}
